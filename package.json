{
  "name": "kogoro",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/cli/index.ts --outdir dist --target bun",
    "check": "npx tsc --noEmit",
    "test": "bun test",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check-all": "biome check . && bun run check && bun test",
    "sandcastle": "npx tsx .sandcastle/main.ts",
    "start": "bun run src/cli/index.ts"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.5.10",
    "@biomejs/biome": "^2.4.15",
    "@types/bun": "latest",
    "@types/yargs": "^17.0.35",
    "lefthook": "^2.1.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "yargs": "^18.0.0"
  }
}
