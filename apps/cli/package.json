{
  "name": "@kogoro/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "kogoro": "src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "build:bin": "bun build --compile ${TARGET:+--target $TARGET} src/index.ts --outfile dist/${OUTFILE:-kogoro}",
    "check": "tsc --noEmit",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@clack/prompts": "^1.5.0",
    "@kogoro/core": "workspace:*",
    "@kogoro/plugins": "workspace:*",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/yargs": "^17.0.35"
  }
}
